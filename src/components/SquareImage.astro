---
const { src, alt, className = "", texto = "" } = Astro.props;
---

<div class={`${className} w-auto h-auto lg:h-[750px] flex flex-col items-center justify-center overflow-hidden`}>
  <img src={src} alt={alt} class="w-full md:w-auto h-[500px] object-cover object-center transition-transform duration-500 ease-in-out transform hover:scale-90 cursor-pointer" />
  <h2 class="font-jakarta text-2xl font-bold text-gray-600 min-h-[50px] flex items-center justify-center">{texto}</h2>
</div>